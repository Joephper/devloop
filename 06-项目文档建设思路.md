# 06 · 项目文档建设思路

> 回答：**"项目 `docs/` 目录该怎么建，才能既对人类友好、又对 AI 友好？"**

---

## 一、AI 友好文档的四个特征

| 特征 | 说明 | 反例 |
|------|------|------|
| **结构化** | 标题层级清晰、表格化、列表化 | 一整段散文 |
| **可索引** | 有总目录、模块间交叉链接 | 扁平一堆 md |
| **状态化** | 含"状态字段"（待确认/已确认/已落地） | 无任何时序标记 |
| **增量化** | 新迭代只改差异部分，保留历史 | 每次整文件重写 |

---

## 二、推荐目录结构

```text
docs/
├── README.md                          # 文档中心首页（导航）
├── overview/                          # 项目级文档
│   ├── architecture.md                # 技术选型、架构图、分层
│   ├── database.md                    # DDL、ER、索引、分表
│   ├── api.md                         # 全量 API 规范
│   ├── deployment.md                  # 开发 & 部署
│   ├── user-guide.md                  # 用户指南
│   └── ...                            # 其他跨模块文档
├── modules/                           # 功能模块文档
│   ├── F01-user-sync/
│   │   ├── PRD.md                     # 产品需求
│   │   └── TECH.md                    # 技术方案
│   ├── F02-token-mgmt/
│   │   ├── PRD.md
│   │   └── TECH.md
│   └── ...
└── changelog/                         # 迭代记录
    ├── LATEST.md                      # 当前迭代（每次覆盖）
    ├── f11-device-flow.md             # 历史 changelog（按迭代号归档）
    └── f12-admin-login.md
```

### 2.1 为什么这样拆

- `overview/`：**跨模块**的知识，稳定、低频更新
- `modules/`：**按模块**组织，PRD 稳、TECH 随实现变动
- `changelog/`：**按时间**切片，记录"为什么改"

### 2.2 模块目录命名

推荐使用 `F{序号}-{英文名}` 形式，如 `F01-user-sync`、`F12-admin-login`：

- 序号让列表稳定可排序
- 英文名便于 AI 在代码和文档间做跳转
- 与 `docs/README.md` 的模块表一致

---

## 三、docs/README.md 的作用（文档中心）

这是**整个项目的门面**，也是 AI 第一个会读的文件。必须包含：

1. **项目一句话定位**
2. **核心价值/卖点**
3. **文档导航表**（overview + modules）
4. **代码目录索引**（每个文件→对应模块）
5. **非功能需求**（性能、安全、可用性、兼容性）
6. **版本规划**（v1.0 / v1.1 / ...）

→ 参考 `sailvan-opengate-backend/docs/README.md`，或模板 [templates/docs/docs-README.md](./templates/docs/docs-README.md)。

---

## 四、changelog 建设思路

### 4.1 LATEST.md 的"双重身份"

`docs/changelog/LATEST.md` 同时承担两个角色：

- **当前迭代的变更清单**（新建、待确认）
- **随时可覆盖的最新迭代快照**

### 4.2 完整流转

```text
新迭代开始 → PM 覆盖 LATEST.md（状态=待确认）
用户确认 → 状态=已确认
代码落地 → 状态=已落地
迭代关闭 → 原 LATEST.md 另存为 changelog/{迭代名}.md
新一轮迭代 → 再次覆盖 LATEST.md
```

### 4.3 必含五大章节

1. 变更概述
2. 影响范围（表格：类型 + 模块 + 变更性质）
3. 详细变更（每个模块逐条展开，含验收标准）
4. 项目级文档变更（涉及的 overview 更新）
5. 开放问题（待确认/风险点）

→ 模板 [templates/docs/changelog-LATEST.md](./templates/docs/changelog-LATEST.md)。

---

## 五、PRD 建设思路

### 5.1 定位

PRD 回答"**做什么 / 为什么做**"，不写"**怎么做**"。

### 5.2 必含要素

- 模块概述 & 用户场景
- 功能清单 & 用户故事
- 验收标准（可测试）
- 页面/交互（如有前端）
- 业务规则 & 异常处理
- 数据需求（字段清单，不写表结构）
- 非功能需求（性能、权限）
- 依赖 & 开放问题

### 5.3 反模式

- **不写技术选型** —— 那是 TECH 的事
- **不写实现细节** —— 那是代码的事
- **验收标准不能主观** —— "快一点"应该写"P95 < 200ms"

→ 模板 [templates/docs/module-PRD.md](./templates/docs/module-PRD.md)。

---

## 六、TECH 建设思路

### 6.1 定位

TECH 回答"**怎么做**"，是**实现蓝图**。

### 6.2 必含要素

- 方案概述 & 关键决策
- 模块/组件设计（Mermaid 类图、时序图）
- 数据库设计（DDL、索引、分表策略）
- API 设计（路径、请求/响应、错误码）
- 核心算法/流程（Mermaid 流程图）
- 安全设计（鉴权、脱敏、加密）
- 配置项（环境变量清单）
- 部署 & 运维（迁移、回滚）
- 依赖 & 性能分析

### 6.3 Mermaid 的重要性

**AI 对 Mermaid 非常友好**，能直接理解和修改。强烈建议：

- 架构图用 `flowchart`
- 时序图用 `sequenceDiagram`
- 类图用 `classDiagram`
- 状态图用 `stateDiagram-v2`
- ER 图用 `erDiagram`

→ 模板 [templates/docs/module-TECH.md](./templates/docs/module-TECH.md)。

---

## 七、测试报告与审查意见

### 7.1 测试报告

建议放在 `docs/changelog/{迭代}-test-report.md` 或 `backend/tests/reports/`：

- 测试范围（用例数、通过/失败/阻塞）
- 关键缺陷列表
- 测试结论（建议发布 / 不建议 + 理由）

→ 模板 [templates/docs/test-report.md](./templates/docs/test-report.md)。

### 7.2 审查意见

可作为 MR 描述主体，也可存 `docs/changelog/{迭代}-review.md`：

```markdown
## Findings
## Open Questions
## Merge Recommendation
```

→ 模板 [templates/docs/merge-review.md](./templates/docs/merge-review.md)。

---

## 八、文档的命名与版本规范

### 8.1 命名

| 对象 | 命名规范 | 示例 |
|------|---------|------|
| 模块目录 | `F{xx}-{短名}` | `F05-gateway-proxy` |
| PRD/TECH | `PRD.md` / `TECH.md`（固定） | `modules/F05-gateway-proxy/PRD.md` |
| changelog | `LATEST.md` + 归档用小写连字符 | `f12-admin-login.md` |
| overview | 小写连字符 | `deployment-production.md` |

### 8.2 版本

- `docs/README.md` 顶部维护版本号和更新日期
- 每份 PRD/TECH 可选"历史"章节记录修订
- 大版本升级（v1→v2）时归档 `docs/README.md` 为 `docs/archive/README-v1.md`

---

## 九、文档与代码的强绑定

### 9.1 代码目录索引

在 `docs/README.md` 的"代码目录索引"节，标注每个文件对应哪个模块：

```text
backend/app/
├── routers/
│   ├── admin_users.py        # → F01 用户管理
│   ├── admin_tokens.py       # → F02 Token 管理
│   └── ...
```

这让 AI 看到代码文件能立刻知道关联的 PRD/TECH。

### 9.2 代码注释引用文档

在关键文件顶部注释里引用对应文档：

```python
# 本模块对应 docs/modules/F05-gateway-proxy/TECH.md
# 变更历史见 docs/changelog/
```

---

## 十、文档的持续演进

### 10.1 按迭代更新规则

| 变更类型 | 动作 |
|---------|------|
| 新增功能模块 | 新建 `modules/F{xx}/PRD.md` + `TECH.md`；更新 `docs/README.md` 模块表 |
| 已有模块优化 | 直接改对应 PRD + TECH，changelog 记录差异 |
| 跨模块架构变更 | 改 `overview/architecture.md` + 多个 TECH；changelog 第四节详述 |
| 仅 Bug 修复 | 不一定改 PRD/TECH，但 changelog 要记 |

### 10.2 保持"单一事实来源"

- **一件事只有一个文档是权威**
- 其他文档最多做 **一句话引用 + 链接**，不要复制内容
- 典型例子：API 路径只写在 `overview/api.md`，TECH 里只说"详见 API 文档"

---

## 十一、参考样板

| 项目 | 路径 | 亮点 |
|------|------|------|
| sailvan-opengate-backend | `docs/` | 12 个模块完整 PRD+TECH，8 份 overview |

可直接对照该项目结构搭建新仓库的 `docs/`。

---

## 十二、延伸阅读

- [03 - 闭环方案设计](./03-闭环方案设计.md)（文档驱动闭环）
- [07 - 落地实施 Checklist](./07-落地实施Checklist.md)
- [templates/docs/](./templates/docs/)
