# 08 · 最佳实践与反模式

> 实战中踩过的坑与对应的推荐做法。

---

## 一、需求阶段（product-manager）

### ✅ 最佳实践

- **先复述再动笔**。让 AI 用自己的话重述一次需求，立刻暴露理解偏差
- **表格化影响范围**。"新增/修改 | 模块 | 变更性质 | 说明"四列结构清晰
- **每条变更都带验收标准**。否则 QA 阶段会没有依据
- **显式写"开放问题"**。宁可暴露，不要埋雷

### ❌ 反模式

- **跳过沟通直接写 PRD**：AI 会脑补用户需求
- **一个 changelog 里揉太多不相关功能**：建议拆成多个迭代
- **PRD 里写技术选型**："用 Redis 缓存"是 TECH 的事，PRD 只说"需要缓存"
- **验收标准主观化**："体验要好" → 写 "首屏 LCP < 2.5s"

---

## 二、技术阶段（tech-developer）

### ✅ 最佳实践

- **TECH.md 先于代码**。哪怕只有 10 行粗设计，也强于零设计
- **Mermaid 图超过文字**。架构图、时序图都用 Mermaid，AI 改图比改文字更准
- **显式写"关键决策"和"被否决的选项"**。未来回溯需求
- **配置项集中成一张表**。环境变量 / 运行参数统一列出

### ❌ 反模式

- **TECH 直接拷贝代码**：TECH 是蓝图不是实现
- **忽略数据库迁移**：`alembic revision` 相关动作必须写进 TECH
- **设计与实现不一致还不更新 TECH**：merge-review 会卡
- **把安全设计留到最后补**：鉴权、脱敏、速率限制应在 TECH 第一版就有

---

## 三、并行开发（parallel-dev）

### ✅ 最佳实践

- **先画"依赖图"**再拆分。相互依赖强的模块不能并行
- **子模块有"完成标准"**。"能跑/联调通过/测试覆盖率 N%"三选一
- **端口注册表永远维护**。`.shared_cache/shared-data/port-registry.json`
- **worktree 命名与分支一致**：`../{repo}-{branch}`

### ❌ 反模式

- **不检查 worktree 前置就开工**：导致分支切换混乱
- **子模块用同一端口**：冲突时 debug 困难
- **联调延后到 QA 阶段**：应该在 parallel-dev 里先联调再交 QA

---

## 四、测试阶段（qa-tester）

### ✅ 最佳实践

- **先给测试矩阵，再写脚本**。让用户看看覆盖面是否够
- **每个测试用例独立**。不依赖执行顺序
- **覆盖边界 > 覆盖快乐路径**。Happy Path AI 自己写得很好，边界才是 Bug 聚集地
- **Bug 必带复现步骤**。"1. ... 2. ... 3. ..."格式

### ❌ 反模式

- **测试脚本和代码耦合过深**：改实现就得改测试
- **只跑不报告**：必须产出测试报告，明确"建议发布 / 不建议"
- **用 Skip 掩盖问题**：Skip 的 case 必须在报告里列出

---

## 五、审查阶段（merge-review）

### ✅ 最佳实践

- **看完整 diff，不只看最新 commit**。`git diff target...source`
- **哪怕无问题也写"未发现阻塞"**。留下审查痕迹
- **Findings 标严重级**：blocker / major / minor / nit
- **Open Questions 单独一节**。不要和 Findings 混在一起

### ❌ 反模式

- **把 merge-review 写成"总结型文案"**：应该是问题清单
- **审查发现问题却直接让 git-workflow 合**：必须退回 tech-developer

---

## 六、Git 执行（git-workflow）

### ✅ 最佳实践

- **每次变更后刷新 `.shared_cache/git/*.json`**
- **合并到 `pre` 和 `master` 两道用户确认**
- **`master` 只能从 `pre` 合并**，违者直接拒绝
- **tag 命名统一规范**（semver：v1.4.2）

### ❌ 反模式

- **用 `git push --force`**：master 绝对禁止
- **冲突自动解决后静默提交**：必须人工确认
- **跳过 merge-review 直接合**：Rule 层应该拒绝

---

## 七、文档体系

### ✅ 最佳实践

- **单一事实来源**。API 路径只在 `overview/api.md` 写一次
- **代码目录索引放 README 里**。让 AI 一看就知道代码与模块映射
- **版本号放在 `docs/README.md` 顶部**
- **历史 changelog 归档** `changelog/{迭代名}.md`

### ❌ 反模式

- **overview 和 TECH 内容重复**：TECH 改了 overview 没跟着改
- **changelog 不归档**：迭代多了找不到历史
- **文档和代码目录映射错位**：AI 按老索引找文件

---

## 八、Rules & Skills

### ✅ 最佳实践

- **Rule 要短、Skill 可长**
- **全局放通用、项目放专属**
- **明确"不负责什么"**。每个 Skill 都要写边界
- **frontmatter 的 description 用中英关键词**。中英都容易匹配触发

### ❌ 反模式

- **一个 Rule 文件写 300 行**：AI 难以全文遵守
- **项目 Rule 重复全局 Rule**：维护成本翻倍
- **Skill 里写复杂条件分支**：AI 难以稳定执行

---

## 九、人机协作

### ✅ 最佳实践

- **关键节点用"请确认"触发人类把关**：changelog / PRD / TECH / 测试范围 / 审查 / 合并
- **用户说话要精确**："确认"/"回退"/"改方案"三种明确词
- **AI 说完下一步就停**，不要自作主张连续推进

### ❌ 反模式

- **用户一句"好的"就让 AI 跑完全流程**：真有问题来不及拦
- **AI 不等用户确认就进下一阶段**：违反 Gates 规则
- **AI 遇到不确定时自己假设**：必须追问

---

## 十、团队协作

### ✅ 最佳实践

- **所有人遵守同一套 Rules + Skills**
- **定期（月）回顾 changelog 归档，提炼共同经验**
- **把项目样板作为新仓库的 "seed"**
- **新人 onboarding 先看 `docs/README.md`**

### ❌ 反模式

- **每人定制自己的 Rule**：导致协作混乱
- **口头沟通绕过文档**：下次 AI 又失忆
- **MR 里不留审查意见**：失去审计证据

---

## 十一、性能与成本

### ✅ 最佳实践

- **长文档分章节读**：不让 AI 一口气读 2000 行
- **用 Grep/SemanticSearch 定位再读**：代替全文扫描
- **缓存高频信息**：`.shared_cache/` 避免重复 git 命令
- **并行的 Skill 调用批处理**：多个独立任务一次提交

### ❌ 反模式

- **每次重读整个 docs/**：上下文爆炸
- **每次都重新 `git branch -a`**：忽略缓存
- **单线程串行读 10 个文件**：应该一次并发读

---

## 十二、快速自检

当你觉得 AI 表现不对，按以下顺序排查：

```mermaid
flowchart TD
    A[AI 表现不对] --> B{是否有 Rules?}
    B -->|否| B1[补全 .cursor/rules/]
    B -->|是| C{Rules 是否触发?}
    C -->|否| C1[检查 alwaysApply/globs]
    C -->|是| D{Docs 是否齐全?}
    D -->|否| D1[补 README/changelog/PRD]
    D -->|是| E{Skills 是否安装?}
    E -->|否| E1[确认 ~/.claude/skills/]
    E -->|是| F{对话是否清晰?}
    F -->|否| F1[用"请交给 product-manager"这种明确指令]
    F -->|是| G[到知识库开 issue 提炼新反模式]
```

---

## 十三、延伸阅读

- [07 - 落地实施 Checklist](./07-落地实施Checklist.md)
- [skills-reference/](./skills-reference/)
