# 01 · 核心理念与架构

> 回答一个问题：**"为什么要把 AI 编程做成一套工作流？"**

---

## 一、问题的来源

直接让 AI 写代码常见的痛点：

| 痛点 | 表现 |
|------|------|
| 上下文遗失 | 新开会话 AI 不记得上次的设计、遗漏之前已决策的事项 |
| 阶段跳跃 | 用户说"加个功能"，AI 立刻开写代码，缺需求对齐与技术设计 |
| 产物散落 | 聊天记录里散落着"需求、方案、测试脚本"，无结构化沉淀 |
| 审查缺失 | AI 自己改、自己过，没有合并前审查环节 |
| 重复驯化 | 每个新项目都要重新教 AI 一遍"我们是怎么做的" |
| 合并风险 | 直接 push 到 master、忘记打 tag、漏跑测试 |

核心原因是 —— **AI 缺乏一套持久、结构化、可审计的工作流。**

---

## 二、解题思路：三层协同模型

我们用 **Rules + Skills + Docs** 三层协同来补齐：

```text
┌──────────────────────────────────────────────────────┐
│                 用户输入 / 自然语言需求                 │
└─────────────────────┬────────────────────────────────┘
                      ▼
┌──────────────────────────────────────────────────────┐
│  Rules 层（硬约束）                                    │
│  ├─ 全局 user_rules：跨项目统一的路由 & 门禁           │
│  └─ 项目 .cursor/rules/：项目增强 & 本地定制           │
│     · workflow-stage-routing.mdc  → 路由到哪个 Skill  │
│     · workflow-gates-and-handoffs.mdc → 不得跳步       │
│     · shared-cache.mdc              → 缓存约定         │
└─────────────────────┬────────────────────────────────┘
                      ▼
┌──────────────────────────────────────────────────────┐
│  Skills 层（可组合的工作流）                           │
│  · delivery-orchestrator  总控编排                    │
│  · product-manager        需求梳理                    │
│  · tech-developer         技术设计与编码              │
│  · parallel-dev           多模块并行                  │
│  · qa-tester              测试验收                    │
│  · merge-review           合并审查                    │
│  · git-workflow           Git 执行                    │
│  · auto-programming-workflow  跨项目入口              │
└─────────────────────┬────────────────────────────────┘
                      ▼
┌──────────────────────────────────────────────────────┐
│  Docs 层（可被 AI 读取的项目记忆）                     │
│  · docs/changelog/LATEST.md   当前迭代变更            │
│  · docs/modules/Fxx/PRD.md    模块产品需求            │
│  · docs/modules/Fxx/TECH.md   模块技术方案            │
│  · docs/overview/*.md         项目级文档              │
│  · .shared_cache/git/*.json   跨分支持久化缓存        │
└──────────────────────────────────────────────────────┘
```

### 1. Rules 层 —— 约束 AI 的行为边界

Rules 是每次对话都会自动注入的**硬约束**，回答三个问题：

- **路由**：当前这句话应该交给哪个 Skill？
- **门禁**：进入下一阶段需要满足什么条件？
- **回退**：失败时应该退回哪一阶段？

> Rules 是"**宪法**"——必须遵守、不可跳过。

两类 Rules：

| 类型 | 位置 | 作用范围 | 示例 |
|------|------|---------|------|
| 全局 user_rules | Cursor/Claude 用户设置 | 所有项目 | "Always respond in 中文" |
| 项目规则 | `.cursor/rules/*.mdc` | 仅当前仓库 | `workflow-stage-routing.mdc` |

### 2. Skills 层 —— 可组合的角色化工作流

每个 Skill 是一个"角色"，封装：

- 该角色的**职责边界**（做什么、不做什么）
- 该角色的**工作流程**（先后顺序）
- 该角色的**产出格式**（changelog 模板、PRD 模板等）
- 与其他 Skill 的**交接规则**

> Skills 是"**方法论**"——告诉 AI 某个阶段具体该怎么做。

### 3. Docs 层 —— AI 的项目记忆

Docs 是沉淀物，也是 AI 下一次对话的上下文：

- **changelog** = 迭代日记，当前要做什么
- **PRD** = 产品需求，"是什么 / 为什么"
- **TECH** = 技术方案，"怎么做"
- **overview/** = 项目架构、数据库、API、部署等全局信息

> Docs 是"**大脑长期记忆**"——超越单次会话，跨分支、跨时间可复用。

---

## 三、为什么这三层缺一不可

| 只有 Rules，没有 Skills | 只有 Skills，没有 Rules | 只有 Rules+Skills，没有 Docs |
|------------------------|------------------------|------------------------------|
| 有约束但缺方法，AI 知道要分阶段但不会做 | 有方法但易跳步，AI 会技痒提前写代码 | 有流程但没沉淀，下次会话又得从零开始 |

三者缺一：

- **缺 Rules**：AI 随心所欲，跳阶段、漏审查
- **缺 Skills**：AI 没有现成的"操作手册"，每次都要临场发挥
- **缺 Docs**：每次 session 失忆，项目知识无法积累

---

## 四、本方案的核心收益

| 维度 | 传统写法 | 本方案 |
|------|---------|--------|
| 首次接入成本 | 低（直接开写） | 中（要建 docs 骨架） |
| 长期维护成本 | 高（聊天记录散落） | 低（文档可追溯） |
| 跨会话一致性 | 差（每次重讲） | 高（AI 读 docs 即恢复上下文） |
| 跨项目复用 | 不可复用 | 复用 Skills + 模板 |
| 合并安全性 | 依赖人 | Rules 强制审查+确认 |
| 可审计性 | 只有 git log | changelog + PRD + TECH + 审查意见全链路 |

---

## 五、与传统软件工程的关系

这套方案不是"重新发明软件工程"，而是 **把已有软件工程实践"AI 原生化"**：

| 传统实践 | AI 原生化对应 |
|---------|-------------|
| 产品 PRD 文档 | `docs/modules/Fxx/PRD.md`（AI 可读结构） |
| 技术设计文档 | `docs/modules/Fxx/TECH.md`（Mermaid 图 + 表格） |
| 代码评审 | `merge-review` Skill + 阻塞规则 |
| CI/CD | `git-workflow` Skill + 分支/标签约束 |
| Code of Conduct | `workflow-gates-and-handoffs.mdc` |
| 项目 Wiki | `docs/overview/` + `docs/README.md` 导航 |

---

## 六、延伸阅读

- [02 - 开发流程总览](./02-开发流程总览.md)
- [03 - 闭环方案设计](./03-闭环方案设计.md)
